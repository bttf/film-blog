import {
  shortDate
} from 'film-blog/helpers/short-date';

module('ShortDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = shortDate(42);
  ok(result);
});
